<li id="<%= feed_item.id %>">
	<%= link_to gravatar_for(feed_item.user), feed_item.user %>
	<span class="user">
		<%= link_to feed_item.user.name, feed_item.user %>
	</span>
	<span class="content"><%= feed_item.content %></span>
	<span class="timestamp">Publicado <%= time_ago_in_words(feed_item.created_at) %> atras. </span>
	<% if current_user?(feed_item.user) %>
		<%= link_to "borrar", feed_item, method: :delete, confirm: "Estas seguro?", title: feed_item.content %>
￼	<% end %>
</li>